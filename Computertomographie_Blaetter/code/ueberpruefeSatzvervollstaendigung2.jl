function ueberpruefeSatzvervollstaendigung2(c,d)

if cmp(c,"senkrecht") == 0 #Null bedeutet richtig, stimmt überein
    if cmp(d,"Normalenvektor") == 0 
        println("Eure Antwort für Wort1 und Wort2 sind beide korrekt!");
    elseif cmp(d,"normalenvektor") == 0
        println("Eure Antwort für Wort1 und Wort2 sind beide korrekt!");
    elseif cmp(d,"normalen vektor") == 0
        println("Eure Antwort für Wort1 und Wort2 sind beide korrekt!");
    elseif cmp(d,"Normalen Vektor") == 0
        println("Eure Antwort für Wort1 und Wort2 sind beide korrekt!");
    elseif cmp(d,"normalen Vektor") == 0
        println("Eure Antwort für Wort1 und Wort2 sind beide korrekt!");
    elseif cmp(d,"Normalen vektor") == 0
        println("Eure Antwort für Wort1 und Wort2 sind beide korrekt!");
    else
        println("Eure Antwort für Wort1 ist korrekt, nicht aber die für Wort2. Lest euch noch einmal den Satz durch und überprüft eure Rechtschreibung.");
    end
elseif cmp(c,"Senkrecht") == 0 #Null bedeutet richtig, stimmt überein
    if cmp(d,"Normalenvektor") == 0 
        println("Eure Antwort für Wort1 und Wort2 sind beide korrekt!");
    elseif cmp(d,"normalenvektor") == 0
        println("Eure Antwort für Wort1 und Wort2 sind beide korrekt!");
    elseif cmp(d,"normalen vektor") == 0
        println("Eure Antwort für Wort1 und Wort2 sind beide korrekt!");
    elseif cmp(d,"Normalen Vektor") == 0
        println("Eure Antwort für Wort1 und Wort2 sind beide korrekt!");
    elseif cmp(d,"normalen Vektor") == 0
        println("Eure Antwort für Wort1 und Wort2 sind beide korrekt!");
    elseif cmp(d,"Normalen vektor") == 0
        println("Eure Antwort für Wort1 und Wort2 sind beide korrekt!");
    else
        println("Eure Antwort für Wort1 ist korrekt, nicht aber die für Wort2. Lest euch noch einmal den Satz durch und überprüft eure Rechtschreibung.");
    end
elseif cmp(c,"orthogonal") == 0 #Null bedeutet richtig, stimmt überein
    if cmp(d,"Normalenvektor") == 0 
        println("Eure Antwort für Wort1 und Wort2 sind beide korrekt!");
    elseif cmp(d,"normalenvektor") == 0
        println("Eure Antwort für Wort1 und Wort2 sind beide korrekt!");
    elseif cmp(d,"normalen vektor") == 0
        println("Eure Antwort für Wort1 und Wort2 sind beide korrekt!");
    elseif cmp(d,"Normalen Vektor") == 0
        println("Eure Antwort für Wort1 und Wort2 sind beide korrekt!");
    elseif cmp(d,"normalen Vektor") == 0
        println("Eure Antwort für Wort1 und Wort2 sind beide korrekt!");
    elseif cmp(d,"Normalen vektor") == 0
        println("Eure Antwort für Wort1 und Wort2 sind beide korrekt!");
    else
        println("Eure Antwort für Wort1 ist korrekt, nicht aber die für Wort2. Lest euch noch einmal den Satz durch und überprüft eure Rechtschreibung.");
    end
elseif cmp(c,"Orthogonal") == 0 #Null bedeutet richtig, stimmt überein
    if cmp(d,"Normalenvektor") == 0 
        println("Eure Antwort für Wort1 und Wort2 sind beide korrekt!");
    elseif cmp(d,"normalenvektor") == 0
        println("Eure Antwort für Wort1 und Wort2 sind beide korrekt!");
    elseif cmp(d,"normalen vektor") == 0
        println("Eure Antwort für Wort1 und Wort2 sind beide korrekt!");
    elseif cmp(d,"Normalen Vektor") == 0
        println("Eure Antwort für Wort1 und Wort2 sind beide korrekt!");
    elseif cmp(d,"normalen Vektor") == 0
        println("Eure Antwort für Wort1 und Wort2 sind beide korrekt!");
    elseif cmp(d,"Normalen vektor") == 0
        println("Eure Antwort für Wort1 und Wort2 sind beide korrekt!");
    else
        println("Eure Antwort für Wort1 ist korrekt, nicht aber die für Wort2. Lest euch noch einmal den Satz durch und überprüft eure Rechtschreibung.");
    end
else
    if cmp(d,"Normalenvektor") == 0 
        println("Eure Antwort für Wort2 ist korrekt, nicht aber für Wort1.");
    elseif cmp(d,"normalenvektor") == 0
        println("Eure Antwort für Wort2 ist korrekt, nicht aber für Wort1.");
    elseif cmp(d,"normalen vektor") == 0
        println("Eure Antwort für Wort2 ist korrekt, nicht aber für Wort1.");
    elseif cmp(d,"Normalen Vektor") == 0
        println("Eure Antwort für Wort2 ist korrekt, nicht aber für Wort1.");
    elseif cmp(d,"normalen Vektor") == 0
        println("Eure Antwort für Wort2 ist korrekt, nicht aber für Wort1.");
    elseif cmp(d,"Normalen vektor") == 0
        println("Eure Antwort für Wort2 ist korrekt, nicht aber für Wort1.");
    else
        println("Eure Antworten für Wort1 und Wort2 sind noch nicht korrekt. Lest euch noch einmal den Satz durch und überprüft eure Rechtschreibung.");
    end
end



end